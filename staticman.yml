comments:

  branch: "master"

  commitMessage: "Add Staticman data"

  filename: "{@id}"

  format: "yaml"

  generatedFields:
    datetime:
      type: date
      options:
        format: "timestamp"
    id:
      type: slugify


  path: "source/_data/comments/{options.path}"

  requiredFields: ["name", "message"]
